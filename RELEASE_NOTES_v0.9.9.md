# TraeJumper v0.9.9 Release Notes

发布日期：2026-08-19

## 🎉 版本概述

本版本是 **Dashboard 仪表盘重构 + 积分体系完整支持** 的重要更新，融合远端 main 分支重构成果，统一账号管理与仪表盘两处的积分/配额双模式展示，并修复多个启动加载与 UI 稳定性问题。

## ✨ 新功能

### 1. Dashboard 仪表盘全面重构
- 欢迎横幅 + 客户端徽章（显示当前管理的 Trae 变体名称）
- 四张统计卡：总积分/总配额、已使用、剩余可用、平均剩余
- 饼图可视化使用量分布（基于 Recharts 3）
- 套餐分布饼图
- 账号概览预览卡片（前 4 个账号进度条 + 预警色）

### 2. 积分体系完整支持
- Dashboard 自动检测 `is_credits_billing` 字段，积分/配额双模式智能切换
- AccountCard 列表底部展示积分到期信息：`[X]积分将于[日期]到期` + `最后到期 [日期]`
- AccountListItem 同步支持积分显示
- 套餐到期时间（`plan_expire_time`）纳入聚合计算

### 3. 批量操作增强
- 工具栏刷新按钮 **默认常显**（无选中时刷新全部，有选中时刷新选中）
- 新增"切换账号"批量按钮（选中单个账号即可切换）
- 工具栏刷新数据范围统一：同时刷新 `usage` + `credits`（与右键菜单一致）

## 🐛 Bug 修复

### 1. 启动加载竞态条件修复（关键）
- **问题**：原有两个独立 useEffect 并行执行 `loadAccounts` 和 `refreshAllTokens`，产生竞态条件，导致启动后账号数据不自动加载，需要手动刷新
- **修复**：合并为单一初始化流程，有序执行：Tauri 就绪等待 → 账号加载（带重试）→ Token 刷新 → 定时器启动

### 2. 卡片视图信息挤压
- **问题**：卡片最小宽度 340px 导致底部时间信息被挤压换行
- **修复**：调整为 460px，并添加 `flex-wrap: nowrap` + `white-space: nowrap` 防止折行

### 3. 列表模式"当前"标签未显示
- **问题**：AccountListItem 未接收 `is_current` 属性
- **修复**：在 props 中添加 `is_current` 字段并条件渲染

### 4. 列表视图底部时间冗余
- 列表视图移除添加时间和积分到期显示，仅保留卡片视图展示

## 🎨 UI/UX 优化

### "当前激活账号"视觉重设计
- 绿色渐变标签 + ✓ 图标前缀（融合方案一与方案三）
- 卡片：绿色顶部条 + 淡绿渐变背景 + 柔和外发光
- 列表项：左侧 3px 绿色边框 + 头像绿色外发光
- "当前"标签位置移至用户名后方（内联显示）

### 积分到期颜色预警
- 已过期 / 0-3 天：红色（#ef4444，加粗）
- 4-7 天：橙色（#f59e0b，中粗）
- > 7 天 / 永久：灰色（默认）
- 符合 WCAG AA 可访问性标准

### 其他视觉调整
- 卡片底部时间字段语义化：`创建于` → `添加于`
- 积分数值样式差异化：加粗 + tabular-nums 对齐
- 点击用户名直接查看详情（无需右键）
- 进度条高度统一为 8px，`flex-shrink: 0` 防压缩
- Dashboard 页面支持纵向滚动

## 🔧 技术改进

### 远端 main 合并
- 合并远端 main 分支（2 个提交），解决 3 个文件冲突：
  - `App.css`：保留滚动支持
  - `App.tsx`：合并批量按钮与常显刷新
  - `Dashboard.tsx`：采用远端结构 + 整合积分支持

### TypeScript 类型修复
- `RewardCreditsEntry` 接口移除不存在的 `id` 字段引用
- Tooltip formatter 类型修正为 `value` 字符串参数

## 📦 安装包

| 平台 | 文件 |
|------|------|
| macOS (Apple Silicon) | `TraeJumper-0.9.9-mac-arm64.dmg` |
| macOS (Intel) | `TraeJumper-0.9.9-mac-x64.dmg` |
| Windows x64 (NSIS) | `TraeJumper-0.9.9-win-x64-setup.exe` |
| Windows x64 (MSI) | `TraeJumper-0.9.9-win-x64.msi` |

## 📋 升级建议

- v0.9.8 及更早版本可直接覆盖安装
- 首次启动会自动加载账号数据（如未加载，点击工具栏"刷新全部"）
- 若 Dashboard 显示异常，请确认已升级到最新版

## 🙏 致谢

感谢所有提交 Issue 和反馈建议的用户。本版本融合了 fork 上游（Yang-505/Trae-Account-Manager）与本地积分体系增强。
